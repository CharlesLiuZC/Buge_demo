# 「不搁」校园 AI 助手 PRD

---

## 1. 封面信息

| 项目 | 内容 |
|------|------|
| **产品名称** | 不搁（Buge） |
| **产品定位** | AI 原生校园行动管家 —— 把群聊里的“信息”自动变成“可执行行动” |
| **目标用户** | 以大学生为核心，重点覆盖课程、社团/学生工作、比赛/科研、实习求职等多事务并行人群 |
| **Demo 形态** | 内网部署 Web 应用（localhost:8080/console），接入 Kimi 2.6 + 腾讯云 24h 终端 + OpenClaw（QQ Bot / 微信 Claw / 飞书机器人） |
| **核心差异化** | 不独立于信息流之外，而是**嵌入群聊场景**，自动监测 DDL、截止时间、任务节点；支持粗筛+细筛双层语义解析，覆盖显式与隐式 DDL 表达 |

---

## 2. 调研洞察与问题定义

### 2.1 核心发现
- **信息入口高度集中**：微信/QQ 群是 90% 以上校园通知的第一落点，但群聊消息密度大、噪声多。
- **遗漏不是记忆力问题**：用户真正漏掉的是“可执行动作、截止时间、材料要求、机会型信息”，而非日程本身。
- **手动整理成本高**：传统待办 App 迁移成本高，用户更希望在原信息流里低摩擦完成任务收纳。
- **最大痛点排序**：
  1. 群消息太多被刷掉（68.2%）
  2. 信息分散在多平台（59.1%）
  3. 看到但没及时记录（52.3%）
  4. 通知太长看不出重点（47.7%）
  5. 多 DDL 冲突（36.4%）

### 2.2 问题定义
大学生每天从 QQ 群、微信群、飞书群接收大量校园通知，现有处理方式依赖人工阅读、筛选、记录和设置提醒，导致 **DDL、报名截止、材料提交、活动机会被系统性遗漏**。需要一个能**自动监测群聊、理解上下文、提取任务并结构化**的 AI 助手。

---

## 3. 用户画像与核心场景

### 3.1 目标用户
- **主用户**：大一至大三本科生，多事务并行（课程+社团+比赛+学生工作）
- **次用户**：班委、学生干部、竞赛参与者、实习求职阶段学生
- **典型特征**：高频使用 QQ/微信群；通知来源多、格式杂、重复转发多；有记录意愿但难以持续维护待办系统

### 3.2 核心使用场景

| 场景 | 描述 |
|------|------|
| **场景一：群聊自动监测** | 用户在 QQ/微信群里看到“5月16日晚上八点，带叶老师参加party”，不搁自动识别时间+任务，提取为日程。 |
| **场景二：隐式 DDL 捕获** | 消息不含“截止/DDL”等字眼，但包含“记得明天下午三点交材料”等祈使句，系统通过粗筛命中→细筛提取。 |
| **场景三：多平台统一** | 用户同时使用 QQ、微信、飞书，不搁通过 Bot 接入三个平台，统一收纳任务。 |
| **场景四：冲突预警** | 多个 DDL 或课程时间重叠时，系统高亮冲突并给出风险提醒。 |
| **场景五：模糊任务暂存** | 用户暂时不确定是否参加活动，将任务暂存到收集栏，之后再安排。 |

---

## 4. 产品方案设计

### 4.1 产品概述
**不搁** 是一个嵌入校园群聊场景的 AI 行动管家。它通过 QQ Bot、微信 Claw、飞书机器人实时监听群聊消息，经过**粗筛（关键词初筛）+ 细筛（正则+规则精准抽取）**双层语义解析，将通知里的可执行任务转化为结构化日程，并支持冲突检测、优先级建议和跨平台统一管理。

### 4.2 产品架构

```
┌─────────────────────────────────────────────────┐
│                    消息入口                        │
│   QQ 群  │  微信群  │  飞书群  │  私聊 Agent       │
└──────────────────┬──────────────────────────────┘
                   │  OpenClaw 消息接入
                   ▼
┌─────────────────────────────────────────────────┐
│               粗筛（关键词初筛）                    │
│  6 大类关键词库匹配：DDL词汇 │ 提交动作 │ 考试节点   │
│  活动/会议 │ 提醒祈使 │ 时间指示词                    │
│  命中任意一类 → 送入细筛                            │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│               细筛（结构化提取）                    │
│  时间提取正则 + 别名映射 → 任务提取模式匹配          │
│  → 输出 {task, deadline, source}                │
│  （可选）Kimi 2.6 LLM 兜底解析                     │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│               任务管理核心                          │
│  日程时间轴 │ 日历视图 │ 收集栏 │ 冲突预警 │ 课程表    │
└─────────────────────────────────────────────────┘
```

### 4.3 核心功能模块

| 模块 | 核心能力 | 解决的问题 |
|------|----------|------------|
| **群聊消息接入** | QQ Bot / 微信 Claw / 飞书机器人实时监听 | 贴近用户原始信息流，降低工具迁移成本 |
| **粗筛引擎** | 6 大类关键词库，命中即进入细筛 | 解决“没有DDL字眼就不会识别”的漏检问题 |
| **细筛引擎** | 正则+规则提取时间+任务，支持 4 种任务模式 | 精准结构化输出，覆盖隐式表达 |
| **AI 兜底解析** | Kimi 2.6 LLM 对未匹配消息进行语义理解 | 处理复杂、非标准表达 |
| **冲突检测** | 任务与任务、任务与课程时间重叠检测 | 解决多 DDL 冲突 |
| **收集栏** | 待定任务暂存，二次安排 | 兼容“暂时不确定”的场景 |
| **日程管理** | 时间轴、日历视图、任务 CRUD | 沉淀为可扫描的行动计划 |

---

## 5. 功能需求明细

### 5.1 P0 必做能力

| 功能 | 说明 |
|------|------|
| **群聊实时监听** | 通过 OpenClaw 接入 QQ/微信/飞书，实时获取群消息 |
| **粗筛关键词匹配** | 6 类关键词库（DDL词汇/提交动作/考试节点/活动会议/提醒祈使/时间指示词），命中任意一类即触发细筛 |
| **细筛结构化提取** | 正则提取时间 + 4 种任务模式（DDL明示/祈使提醒/时间+动作/时间+名词） |
| **任务结构化输出** | 输出标准字段：task、deadline、source、location（可选） |
| **冲突基础检测** | 任务与课程、任务与任务时间重叠时提示 |
| **收集栏暂存** | 支持用户将识别结果暂存，不直接入库 |
| **自然语言查询** | 支持用户通过 Bot 私聊查询今日/本周任务 |

### 5.2 P1 增强能力

| 功能 | 说明 |
|------|------|
| **LLM 兜底解析** | 粗筛命中但细筛未匹配的消息，送 Kimi 2.6 做语义理解 |
| **优先级建议** | 根据 DDL 临近度、任务类型生成 P0/P1/P2 |
| **DDL 倒推排程** | 对截止任务自动预留准备时间 |
| **跨平台任务统一** | QQ/微信/飞书三个来源的任务汇聚到同一视图 |
| **提醒推送** | 通过 Bot 私聊发送 DDL 临近提醒 |
| **已处理标记** | 群聊中已提取的消息标记“已入库” |

### 5.3 P2 后续扩展

- 长通知摘要、材料要求提取
- 图片/海报 OCR 识别
- PDF/公众号信息导入
- 自动生成本周/今晚行动计划
- 一键生成报名/回复文案
- 系统级通知推送
- 账号体系与云端持久化

---

## 6. AI 能力说明

### 6.1 粗筛：关键词初筛引擎

**6 大类关键词库**（完整）：

1. **直接 DDL/截止词汇**：DDL、deadline、截止日期、截止时间、到期、逾期、due date 等
2. **提交/交付动作词**：提交、上交、交付、交作业、交报告、交论文、打印、签字 等
3. **考试/考核时间节点**：考试、测验、月考、期末考、答辩、面试、笔试 等
4. **活动/会议类**：开会、组会、班会、活动、party、比赛、报名、签到 等
5. **提醒/祈使词**：别忘了、记得、务必、一定、提醒大家、注意 等
6. **时间指示词**：今天/明天/后天 + 时刻、下周一/周三、X月X日、上午/下午/晚上 + 钟点

**命中逻辑**：消息中（时间表达式 + 行为动词/任务名词）或（时间表达式 + 明确任务对象），即进入细筛。

### 6.2 细筛：结构化提取引擎

**时间提取**：
- 正则覆盖中文全场景时间表达
- 别名映射表：今晚→当日20:00、明早→次日08:00、下周一上午→下周一09:00 等

**任务提取 4 种模式**（按优先级）：
1. DDL 明示结构：`交/提交/完成 + 任务名 + 截止/DDL`
2. 祈使提醒结构：`别忘了/记得/务必 + 任务描述`
3. 时间 + 动作/事件：`[时间] + 参加/准备/报名 + 任务描述`
4. 时间 + 名词短语：`[时间] + 组会/作业/party`

**输出格式**：
```json
{
  "task": "带叶老师参加party",
  "deadline": "2026-05-16T20:00:00",
  "source": "QQ群消息",
  "location": ""
}
```

### 6.3 LLM 兜底（Kimi 2.6）

- 粗筛命中但细筛无法匹配的消息，送入 LLM 做语义理解
- Prompt 约束输出 JSON Schema，确保结构化
- 异常兜底：超时、解析失败时返回人工确认标记

---

## 7. 数据对象

### 7.1 Task（任务）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| title | string | 任务名称 |
| deadline | datetime | 截止/发生时间 |
| source | string | 来源群聊（QQ群/微信群/飞书群） |
| sourceMessageId | string | 原始消息 ID |
| location | string | 地点（可选） |
| priority | P0/P1/P2 | 优先级 |
| status | pending/done/collected | 状态 |
| isExpired | boolean | 是否已过期 |
| createdAt | datetime | 创建时间 |

### 7.2 KeywordRule（粗筛规则）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 规则 ID |
| category | string | 所属类别（6大类） |
| keywords | string[] | 关键词列表 |
| enabled | boolean | 是否启用 |

### 7.3 ExtractPattern（细筛模式）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 模式 ID |
| priority | number | 匹配优先级（1-4） |
| patternType | string | 模式类型（ddl_explicit/imperative/time_action/time_noun） |
| regex | string | 正则表达式 |

---

## 8. 验收标准

### 8.1 核心验收项

| 验收项 | 标准 |
|--------|------|
| **显式 DDL 识别** | 含“DDL/截止/提交”等词的消息 100% 命中粗筛，正确提取时间与任务 |
| **隐式 DDL 识别** | “5月16日晚上八点，带叶老师参加party” 正确识别，输出结构化任务 |
| **祈使句识别** | “记得明天下午三点交材料” 正确提取 |
| **多平台覆盖** | QQ 群、微信群、飞书群消息均可监听并解析 |
| **防误判** | 纯闲聊、过去时间消息不触发解析 |
| **冲突检测** | 任务与课程时间重叠时给出提示 |
| **解析确认** | 用户可在解析结果中选择“加入日程”或“暂存收集栏” |

### 8.2 效果指标

| 指标 | 目标 |
|------|------|
| 粗筛命中率（含隐式表达） | ≥ 95%（以标注测试集计） |
| 细筛准确率（时间+任务正确） | ≥ 90% |
| 端到端延迟（消息发→解析完成） | ≤ 3 秒 |
| 冲突检测覆盖率 | 任务与课程重叠 100% 检出 |

---

## 9. 创新与差异化

- **双层语义解析**：粗筛+细筛的架构，先广撒网再精准提取，既保证召回又保证精度
- **不是独立 App，而是信息流内嵌入**：用户无需离开 QQ/微信/飞书，在原聊天窗口获得智能解析结果
- **覆盖隐式 DDL**：不依赖“截止/DDL”等显式词，能理解“时间+动作”的自然语言表达
- **多平台统一**：QQ、微信、飞书三端任务汇聚到同一管理视图
- **关键词+规则+LLM 三级递进**：规则处理 80% 标准场景，LLM 兜底复杂语义，兼顾速度与智能

---

## 10. 落地边界

### 10.1 当前（Demo 阶段）
- 内网部署于 localhost:8080/console
- 接入 Kimi 2.6 + 腾讯云 24h 终端 + OpenClaw
- QQ Bot / 微信 Claw / 飞书 Bot 已打通
- 粗筛+细筛规则已就绪，覆盖核心场景
- 任务结构化输出、冲突检测已完成

### 10.2 Demo 边界
- 包含真实账号体系（当前为内网调试）
- 不包含系统级推送（提醒目前为 Bot 私聊消息）
- 不包含图片/PDF/海报 OCR 识别（在上一个项目CampusFLow之中）
- 群聊数据为真实接入但限于调试环境
- 跨设备同步与云端持久化尚未接入

